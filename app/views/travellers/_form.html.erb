<%= form_for :traveller, url: params[:tour_id] ? tour_travellers_path(params[:tour_id]) : travellers_path, method: :post do |f| %>
  <% if @traveller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@traveller.errors.count, "error") %> prohibited this traveller from being saved:</h2>

      <ul>
      <% @traveller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table  style="width:70%; margin-left:10%">
  <tr height="30px">
    <td  width="20%"><%= f.label :name %></td>
    <td><%= f.text_field :name %></td>
  </tr>
  <tr height="30px">
    <td><%= f.label :address %></td>
    <td><%= f.text_field :address %></td>
  </tr>
  <tr height="30px">
    <td><%= f.label :phone %></td>
    <td> <%= f.text_field :phone %></td>
  </tr>
  <tr height="30px">
    <td><%= f.label :device_id %><br></td>
    <td><%= select_tag"traveller[device_id]", options_from_collection_for_select(@devices, "id", "code"),include_blank:"Choose Device"%></td>
  </tr>
  <tr height="30px">
    <td></td>
    <td>  <%= f.submit"Add traveller",class:'btn_medium_blue' %></td>
  </tr>
</table>
<% end %>

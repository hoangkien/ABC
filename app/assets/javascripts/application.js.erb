// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or any plugin's vendor/assets/javascripts directory can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require jquery
//= require jquery_ujs

//= require_tree .
//= require filterrific/filterrific-jquery

$(document).ready(function() {
     var url = window.location.pathname.split("/");
     // alert(url[1]);
      $("#menu_nav ul li."+url[1]+" a").addClass('active');
    //
    //changeGroup();
    //if($("#center .new_form #user_group").val() =='company'){
    //    $("#center .new_form #user_company_id").css("display","");
    //}else{
    //    $("#center .new_form #user_company_id").css("display","none");
    //}
    if($("#center .new_form #user_group").val()=='admin'){
        $("#center .new_form .company").css("display","none");
    }
    $("#center .new_form #user_group").change(changeGroup);
    $('#list').DataTable();
    $('#list1').DataTable();
    $('#feedbacks_search').submit(function () {
        $.get(this.action, $(this).serialize(), null, 'script');
        return false;
    });
    $('#feedbacks_search input').keyup(function () {
      $.get($('#feedbacks_search').attr('action'),$('#feedbacks_search').serialize(), null, 'script');
        var table = $('#example').DataTable( {
            ajax: "data.json"
        } );
      return false;
    });
    //datepicker
    $( "#fillter_created_at" ).datepicker();
    // jQuery('#fillter_created_at').datetimepicker({
     //  lang:'vi',
     //  i18n:{
     //    fn:{
     //     months:[
     //      'Januar','Februar','März','April',
     //      'Mai','Juni','Juli','August',
     //      'September','Oktober','November','Dezember'
     //     ],
     //     dayOfWeek:[
     //      "So.", "Mo", "Di", "Mi", 
     //      "Do", "Fr", "Sa."
     //     ]
     //    }
     //  },
     //  timepicker:false,
     //  format:'d/m/Y'
     // });
    function changeGroup(){
        console.log(2);
        console.log($("#center .new_form #user_group").val());

        if($("#center .new_form #user_group").val() =='company'){
             $("#center .new_form .company").css("display","");
        }else{
            $("#center .new_form .company").css("display","none");
        }
    }
});


